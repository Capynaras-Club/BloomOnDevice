name: release

on:
  push:
    branches: [main]
    tags:
      - "v*.*.*"
  workflow_dispatch:

# Pages deploy needs id-token to mint the deploy token, pages: write to
# publish, contents: write to push tags + create releases.
permissions:
  contents: write
  pages: write
  id-token: write

# A single concurrency group prevents two pushes-to-main from racing the
# `nightly` tag and the Pages deploy.
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  firmware:
    name: Build and publish firmware
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Cache PlatformIO state
        uses: actions/cache@v5
        with:
          path: |
            ~/.platformio/.cache
            ~/.platformio/packages
            ~/.platformio/platforms
            .pio
          key: pio-${{ runner.os }}-${{ hashFiles('platformio.ini') }}
          restore-keys: pio-${{ runner.os }}-

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      - name: Build
        run: pio run -e esp32-2432S024R

      - name: Stage firmware artifacts
        run: |
          mkdir -p release
          cp .pio/build/esp32-2432S024R/firmware.bin   release/
          cp .pio/build/esp32-2432S024R/firmware.elf   release/
          cp .pio/build/esp32-2432S024R/bootloader.bin release/
          cp .pio/build/esp32-2432S024R/partitions.bin release/
          ( cd release && sha256sum * > SHA256SUMS )
          ls -la release

      # ---------------------------------------------------------------
      # GitHub Releases
      # ---------------------------------------------------------------
      - name: Publish tagged release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          files: release/*
          generate_release_notes: true
          fail_on_unmatched_files: true

      - name: Move nightly tag
        if: github.ref == 'refs/heads/main'
        run: |
          git tag -f nightly
          git push -f origin nightly

      - name: Publish nightly pre-release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: nightly
          name: "Nightly build (main @ ${{ github.sha }})"
          body: |
            Automated build of `main` at commit ${{ github.sha }}.
            This tag is moved on every push to `main`.
          prerelease: true
          make_latest: false
          files: release/*
          fail_on_unmatched_files: true

      # ---------------------------------------------------------------
      # GitHub Pages — installer page + firmware co-located so the
      # ESP Web Tools manifest's relative `firmware/*.bin` paths resolve.
      # Files are staged under /install/ so the deployed URL is
      # https://<owner>.github.io/<repo>/install/ — matching the README
      # link and the manifest's relative paths.
      # ---------------------------------------------------------------
      - name: Stage Pages site
        run: |
          mkdir -p pages-site/install/firmware
          cp -r docs/install/. pages-site/install/
          cp release/firmware.bin   pages-site/install/firmware/
          cp release/bootloader.bin pages-site/install/firmware/
          cp release/partitions.bin pages-site/install/firmware/

          # Stamp the deployed installer page with the commit it was
          # built from, so "did I just flash the latest firmware?" is
          # decidable from the browser without a serial monitor.
          SHA_FULL="${GITHUB_SHA}"
          SHA_SHORT="${SHA_FULL:0:7}"
          BUILD_DATE="$(date -u '+%Y-%m-%d %H:%M UTC')"
          sed -i "s|__BUILD_SHA__|${SHA_FULL}|g"        pages-site/install/index.html
          sed -i "s|__BUILD_SHA_SHORT__|${SHA_SHORT}|g" pages-site/install/index.html
          sed -i "s|__BUILD_DATE__|${BUILD_DATE}|g"     pages-site/install/index.html

          ls -la pages-site pages-site/install pages-site/install/firmware

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: pages-site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
