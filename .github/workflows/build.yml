name: build

on:
  push:
    branches:
      - main
      - "claude/**"
  pull_request:
  workflow_dispatch:

jobs:
  firmware:
    name: Compile firmware (esp32-2432S024R)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache PlatformIO state
        uses: actions/cache@v5
        with:
          path: |
            ~/.platformio/.cache
            ~/.platformio/packages
            ~/.platformio/platforms
            .pio
          key: pio-${{ runner.os }}-${{ hashFiles('platformio.ini') }}
          restore-keys: pio-${{ runner.os }}-

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      - name: Build
        run: pio run -e esp32-2432S024R

      - name: Report sizes
        run: pio run -e esp32-2432S024R --target size

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v7
        with:
          name: firmware-esp32-2432S024R
          path: |
            .pio/build/esp32-2432S024R/firmware.bin
            .pio/build/esp32-2432S024R/firmware.elf
            .pio/build/esp32-2432S024R/bootloader.bin
            .pio/build/esp32-2432S024R/partitions.bin
          if-no-files-found: error
